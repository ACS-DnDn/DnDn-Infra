apiVersion: v1
kind: Service
metadata:
  name: dndn-worker
  namespace: dndn-worker
  labels:
    app: dndn-worker
spec:
  type: ClusterIP
  selector:
    app: dndn-worker
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
