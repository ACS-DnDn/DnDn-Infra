apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dndn-worker
  namespace: monitoring
  labels:
    release: kube-prometheus
spec:
  namespaceSelector:
    matchNames:
      - dndn-worker
  selector:
    matchLabels:
      app: dndn-worker
  endpoints:
    - port: metrics
      path: /metrics/
      interval: 30s
